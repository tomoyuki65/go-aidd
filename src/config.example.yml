issue:
  # provider: GitHub (other options may include Notion in the future)
  provider: "GitHub"
  label: "AI DD"
github:
  repository: "owner/repository-name"
task:
  list_page_size: 5
ai:
  # model: Gemini CLI (other options may include Claude Code in the future)
  model: "Gemini CLI"
