issue:
  # Options: Gemini CLI (other options may include Notion in the future)
  provider: "GitHub"
  label: "AI DD"
github:
  repository: "owner/repository-name"
  # Options: ssh, https, github_cli
  clone_type: "ssh"
  clone_branch: "main"
  push_branch_on_complete: true
  create_pr_on_complete: true
  pr_draft: false
task:
  list_page_size: 5
ai:
  # Options: Gemini CLI (other options may include Claude Code in the future)
  type: "Gemini CLI"
  # Set when you want to specify the model (e.g., gemini-2.5-pro、gemini-2.5-flash)
  model: ""
